\name{get.nodes}
\alias{get.nodes}
\title{Get the nodes of a babyRHugin domain}
\description{Get the nodes of a babyRHugin domain}
\usage{get.nodes(dname)}
\arguments{
  \item{dname}{name of babyRHugin domain}
}
\details{Emulates function of the same name in the \code{RHugin} package by calls to \code{gRain} functions}
\value{Character vector of node names.}
\author{Peter J. Green, \email{P.J.Green@bristol.ac.uk}}
\examples{
demo('chest',echo=FALSE)
get.nodes(chest)
get(chest)$states
sapply(get.nodes(chest),function(x) get.parents(chest,x))
}
